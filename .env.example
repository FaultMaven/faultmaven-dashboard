# FaultMaven Dashboard - Environment Configuration
# Copy this file to .env.local and customize for your environment

# ============================================================================
# API Configuration
# ============================================================================

# Backend API Gateway endpoint
# IMPORTANT: For self-hosted deployments, use your SERVER_HOST IP (not localhost!)
# when accessing the dashboard from a different machine than the server.
#
# Examples:
#   - Development (same machine): http://localhost:8090
#   - Self-hosted (LAN access):   http://192.168.0.200:8090
#   - Enterprise:                 https://api.faultmaven.ai
#
# Note: Port 8090 is the API Gateway, which aggregates all microservices.
# Do NOT use individual service ports (8001-8006) - always use 8090.
VITE_API_URL=http://localhost:8090

# ============================================================================
# Upload Limits
# ============================================================================

# Maximum file upload size in megabytes
# - Self-hosted default: 10 MB
# - Enterprise default: 50 MB
#VITE_MAX_FILE_SIZE_MB=10

# ============================================================================
# How to Use These Variables
# ============================================================================
#
# 1. For local development (same machine as backend):
#    - Copy this file: cp .env.example .env.local
#    - Use default: VITE_API_URL=http://localhost:8090
#    - Start dev server: pnpm dev
#
# 2. For local development (different machine):
#    - Copy this file: cp .env.example .env.local
#    - Set VITE_API_URL=http://<YOUR_SERVER_IP>:8090
#    - Example: VITE_API_URL=http://192.168.0.200:8090
#    - Start dev server: pnpm dev
#
# 3. For Docker deployment (managed by docker-compose):
#    - faultmaven-deploy uses SERVER_HOST from .env
#    - Build arg: VITE_API_URL=http://${SERVER_HOST}:8090
#    - No manual configuration needed!
#
# 4. For manual production builds:
#    - Set before build: export VITE_API_URL=http://<YOUR_IP>:8090
#    - Build: pnpm build
#
# IMPORTANT: All VITE_* variables are baked in at BUILD TIME, not runtime.
# - Changing .env.local requires restarting dev server
# - Changing for production requires rebuilding
# - Docker deployments: rebuild container with new SERVER_HOST
