# FaultMaven Dashboard - Environment Configuration
# Copy this file to .env.local and customize for your environment

# ============================================================================
# API Configuration
# ============================================================================

# Backend API endpoint
# - Self-hosted: http://localhost:8000
# - Enterprise: https://api.faultmaven.ai
VITE_API_URL=http://localhost:8000

# ============================================================================
# Upload Limits
# ============================================================================

# Maximum file upload size in megabytes
# - Self-hosted default: 10 MB
# - Enterprise default: 50 MB
#VITE_MAX_FILE_SIZE_MB=10

# ============================================================================
# How to Use These Variables
# ============================================================================
#
# 1. For local development:
#    - Copy this file: cp .env.example .env.local
#    - Modify values as needed
#    - Restart dev server: npm run dev
#
# 2. For production builds:
#    - Set environment variables before build:
#      export VITE_API_URL=https://api.faultmaven.ai
#      npm run build
#
# 3. For Docker deployment:
#    - Pass as build args:
#      docker build --build-arg VITE_API_URL=https://api.faultmaven.ai .
#
# Note: All VITE_* variables are replaced at BUILD TIME, not runtime.
# Changing .env.local requires restarting the dev server or rebuilding.
